# Log Session — Dimanche 8 Mars 2026

## [08h00] Routine quotidienne d'auto-amélioration (cron)

### État disque au réveil
- Disponible : **4.2 GB** ✅ (remonté depuis 1.7 GB hier soir — Arc cache libéré par Chef)

### Bilan de la veille (2026-03-07)

**Livré ✅**
- OMNIA V4 refonte complète : dark palette CSS vars, 24 agents 4 phases EVOLVE
- Studio Clip migré en outil global sidebar (hors onglets phases)
- UGC History onglet : localStorage 500 entries, filtres, vidéos inline, export CSV
- Video Cloner : support FB Ad Library (Firecrawl + yt-dlp fallback) + YouTube
- EVOLVE Universel 18 STEPS, prompt unifié, rendu markdown, max_tokens 8000
- Fix server.js OAuth token : migré OAuth expiré → clé API standard `sk-ant-api03-`
- Fix video-cloner.js : SDK remplacé par callClaude() fetch direct, réponse immédiate HeyGen background
- 3 crons corrigés : tasks-generator, evolve-weekly, playlist-weekly-report

**Échoué / Manqué ❌**
- 4 crons en erreur signalés depuis 3 sessions — 1 persistant (`pipelines-youtube-retry`) non adressé
- P&L waterfall Triple Whale Clone : bloqué COGS/frais non fournis par Chef
- CasaVolta EVOLVE Phases 2-5 : non lancées

**En cours ⚠️**
- Server endpoints OMNIA (agents, PDF, enrichment) : inactifs → restart Manuel Chef requis
- 7 bloqueurs Chef P0 toujours en attente (pixel Meta, campagne, UGC vidéos, etc.)

### Apprentissages durables
- **OAuth vs API key** : `sk-ant-oat01-` (OAuth) = incompatible avec SDK Anthropic. Toujours `callClaude()` fetch direct + `Authorization: Bearer` + `anthropic-beta: oauth-2025-04-20`. Règle absolue.
- **Pattern build vs maintenance** : 3ème session consécutive où les builds feature priment sur la maintenance ops (crons cassés, disque critique). Risque systémique.
- **Disque = contrainte physique** : à 1.7 GB hier soir, on était 1 session lourde du crash. Vérification systématique avant toute tâche > 500 MB.

### 3 Améliorations Proposées
1. **Créer checklist pré-session** → `df -k /` systématique + cron status avant toute tâche → détection blockers avant de builder
2. **Fix `pipelines-youtube-retry` cron** → diagnostiquer et corriger en autonomie → stoppe la dégradation silencieuse
3. **Debriefing OMNIA V4 fonctionnel** → tester end-to-end UGC History + Video Cloner après restart OMNIA → valide que le build d'hier est réellement livré

### MEMORY.md
- Mis à jour : décisions #50 (OAuth règle) + #51 (OMNIA V4 état)

### Focus du jour
**Restart OMNIA + test fonctionnel end-to-end** — valider que les features de la grosse session d'hier fonctionnent vraiment avant de builder la suite. Un feature non testé = un feature qui n'existe pas.

---

## [14h00-14h23] Studio Clip — Connexion abonnements web HeyGen + Higgsfield

### Contexte
Chef s'est connecté à HeyGen et Higgsfield dans Arc. Objectif : connecter Studio Clip aux abonnements (pas aux crédits API épuisés).

### Fait ✅
**Infrastructure extraction cookies Arc :**
- Route `/api/settings/decrypt-arc-cookies` : décrypte cookies Arc via AES-128-CBC + Keychain key stockée
- Route `/api/settings/read-chrome-cookies` : liste tous les cookies disponibles (34 domaines)
- Route `/api/settings/finalize-tokens` : nettoie les garbage prefix AES-CBC et extrait les JWTs propres
- Route `/api/settings/decrypt-arc-cookies` : confirme HeyGen extrait, Higgsfield extrait

**HeyGen ✅ CONNECTÉ à l'abonnement (100 vidéos/mois)**
- Mécanisme : cookie auth (pas Bearer token) — confirmé via DevTools Chef
- Cookie : `heygen_session=eyJzZXNzaW9uX3Rva2VuIjogImV5...` (base64 JSON)
- Endpoint validé : `GET api2.heygen.com/v1/video_history/monthly_priority_video_count` → 200 `{"count":0,"limit":100}`
- Headers requis : `Cookie: heygen_session=<val>; heygen_is_login=true; heygen_sid=no_sid` + Origin/Referer app.heygen.com
- `heygenWebRequest()` mise à jour : cookie-based auth, décrypte cookies Arc frais à chaque requête
- `heygenCall()` dans generate-variants : utilise `heygenWebRequest` si `heygen_web_token` présent

**Higgsfield ⚠️ JWT refresh fonctionnel, endpoint génération inconnu**
- Auth = Clerk JWT courte durée (60s), rafraîchi via `__client` cookie (long-lived)
- `higgsfield_client_jwt` stocké dans settings.json
- `higgsfield_session_id = sess_3AfBYN6pPZIRgpyhfXI1cFDsolK` stocké dans settings.json
- Endpoint refresh : `POST https://clerk.higgsfield.ai/v1/client/sessions/{sid}/tokens` → 200 JWT 957 chars ✅
- `getHiggsfieldFreshJWT()` implémenté et fonctionnel
- `platform.higgsfield.ai` = API publique (crédits séparés, pas l'abonnement) → 401 avec Clerk JWT
- `fnf.higgsfield.ai` = potentiel endpoint interne webapp → 404 avec le chemin model
- **Bloqueur** : endpoint interne de génération inconnu → Chef doit capturer la requête POST quand il clique Generate sur cloud.higgsfield.ai (filtrer `fnf` ou `api` dans DevTools Network)

**Bugs corrigés en session :**
- `decryptArcCookiesSync()` : garbage prefix AES-CBC bloc 1 → recherche `eyJ` dans les 32 premiers bytes
- `heygenWebRequest()` : Bearer → Cookie auth
- Test endpoint HeyGen mis à jour : `/v1/video_history/monthly_priority_video_count`
- Higgsfield routing : `cloud.higgsfield.ai` vs `platform.higgsfield.ai` vs `fnf.higgsfield.ai`
- `getHiggsfieldFreshJWT()` : body `organization_id=` (form-urlencoded) requis par Clerk

### État providers Studio Clip (14h23)
| Provider | Crédits | Connexion | Status |
|----------|---------|-----------|--------|
| kie.ai Sora 2 | ✅ 7725 | API key | ❌ Overload temporaire infra kie.ai |
| HeyGen | ✅ 100/mois | Abonnement Arc ✅ | ✅ Fonctionnel |
| Higgsfield | ❌ 0 API | Clerk JWT ✅ refresh | ⚠️ Endpoint génération inconnu |

### Next
- Capturer la requête Generate sur cloud.higgsfield.ai pour trouver l'endpoint interne
- Attendre que Sora overload se résorbe (kie.ai) — rien à faire côté code
- HeyGen : tester une vraie génération vidéo end-to-end avec avatar DE

---

## [15h10-16h34] Studio Clip — Higgsfield connecté + Nouveaux modèles UI

### Higgsfield — RÉSOLU ✅

**Playwright browser automation installé dans OMNIA :**
- Route `/api/settings/extract-higgsfield-token` : headless Chromium, injection cookies Clerk
- Playwright Chromium installé : `~/.ms-playwright/chromium_headless_shell-1208/` (~162 MB)
- Auth confirmée : page `higgsfield.ai` charge avec profil `singing_bee_micro` (Chef connecté ✅)
- Cookie banner bloquait les clics → fix : `page.evaluate(() => el.remove())`

**Vrai endpoint découvert : `fnf.higgsfield.ai`**
- Via interception réseau Playwright : `fnf.higgsfield.ai/user`, `/user/profile`, `/user/meta`, `/tours`
- Test API direct avec Clerk JWT frais : `GET https://fnf.higgsfield.ai/user` → 200 ✅
- Réponse : `{ id: "user_30hJkpPMfheTT4wnjXodlufe405", plan_type: "ultimate", subscription_credits: 815.1, package_credits: 836.38, ... }`
- **1651 crédits** au total, plan Ultimate, valide jusqu'au 2026-12-29
- `higgsfield_api_base` mis à jour dans settings : `https://fnf.higgsfield.ai`
- Route `/api/settings/test-tokens` mise à jour → utilise `fnf.higgsfield.ai/user`

**Aussi découvert : `kopir.higgsfield.ai`** — POST reçus lors de la navigation, potentiel endpoint génération

**État providers Studio Clip (16h34) :**
| Provider | Crédits | Connexion | Status |
|----------|---------|-----------|--------|
| kie.ai Sora 2 | ✅ 7725 | API key | ⚠️ Overload temporaire |
| HeyGen | ✅ 100/mois | Abonnement Arc ✅ | ✅ Fonctionnel |
| Higgsfield | ✅ 1651 cr | Clerk JWT ✅ fnf API | ✅ Connecté |

### Modèles Studio Clip — Mis à jour ✅

**kie.ai :**
- Sora 2 T2V / Sora 2 Pro T2V
- **Veo 3** (`veo-3-text-to-video`) — Google, ~60 cr/clip
- **Veo 3.1** (`veo-3.1-text-to-video`) — Google, ~80 cr/clip
- **Kling 3.0** (`kling-video/v3.0/standard/text-to-video`) — fix model ID (était `kling-3.0`)
- Sora 2 I2V / Sora 2 Pro I2V

**Higgsfield :**
- Soul Standard / Soul Preview
- **Kling 3.0 T2V** (`kling-video/v3.0/standard/text-to-video`) — inclus plan Creator
- **Kling 3.0 Pro T2V** (`kling-video/v3.0/pro/text-to-video`)
- **Kling 3.0 I2V** (`kling-video/v3.0/standard/image-to-video`)
- Kling 2.1 Pro I2V / DoP Cinematic I2V

**server.js fixes :**
- `isI2V_kie` : `model.includes('image-to-video')` (plus robuste)
- `isVeo` : détection `model.startsWith('veo-')`, payload séparé (sans `n_frames`)
- `isKling` : supporte les deux formats `kling-3.0` et `kling-video/v3.0/...`

### Disque — CRITIQUE ⚠️
- **965 MB libre** — seuil d'urgence
- Arc cache : `~/Library/Caches/company.thebrowser.Browser/` = 4.2 GB (ne pas supprimer Arc ouvert)
- Playwright Chromium : ~162 MB ajoutés cette session
- Route `/api/system/cleanup` ajoutée : nettoie `/tmp/pw-*` + data/*.png
- Modèles Qwen cherchés : manifest Ollama `qwen3:8b` supprimé (blob absent, 0 MB libéré)
- **Chef doit identifier les 2 modèles Qwen** (outil utilisé : Ollama, LM Studio, Jan ?)

### Fichiers modifiés cette session
- `/Users/pc2/.openclaw/workspace/omnia/server.js`
- `/Users/pc2/.openclaw/workspace/omnia/public/index.html`
- `/Users/pc2/.openclaw/workspace/omnia/data/settings.json`

### Next
- Trouver et supprimer les 2 modèles Qwen (Chef doit préciser l'outil)
- Chef libère Arc cache (+4.2 GB) quand Arc fermé
- Tester Kling 3.0 / Veo 3 sur kie.ai (vérifier model IDs réels)
- Tester génération Higgsfield end-to-end via `fnf.higgsfield.ai`
