# Log Session — Samedi 7 Mars 2026

## [08h00] Routine quotidienne d'auto-amélioration (cron)

### Bilan de la veille (2026-03-06)

**Livré ✅**
- OMNIA Command Center refonte V4 : fusion Projets+OKR → structure hiérarchique, 3 vrais objectifs stratégiques définis, bug JS "dead code" résolu, localStorage reset
- EVOLVE Universel refonte complète : 5 onglets → 1 bouton, formulaire enrichi (description, multi-marchés, URLs concurrents dynamiques), prompt 18 STEPS, max_tokens 8000, rendu markdown custom
- Fix Mission Control iframe (LaunchAgent recréé, X-Frame-Options corrigé)
- CasaVolta Research V2 (17700 chars) → sub-agent Python V3 lancé

**Échoué / Manqué ❌**
- 4 crons en erreur persistent : `tasks-generator`, `pipelines-youtube-retry`, `evolve-weekly`, `playlist-weekly-report` — toujours non adressés
- P&L waterfall Triple Whale Clone : bloqué sur COGS/frais fixes de Chef (non fournis)
- CasaVolta Research V3 en cours (non confirmé livré)

**En cours ⚠️**
- EVOLVE Universel : phases 2-5 CasaVolta à lancer
- Bloqueurs Chef P0 : 7 items toujours non débloqués (vidéos UGC, pixel Meta, campagne, etc.)

### WIN DU JOUR
EVOLVE Universel refonte majeure : le moteur research passe de 3 onglets bricolés à un prompt unifié 18 STEPS avec rendu markdown professionnel. C'est la fondation pour lancer EVOLVE sur n'importe quel produit.

### FAIL DU JOUR
Les 4 crons en erreur persistent — 3ème session consécutive sans correction. Cause racine : déprioritisés systématiquement face aux builds. Risque : routines silencieusement dégradées.

### PATTERN IDENTIFIÉ
Le pattern de 3 jours se confirme : on build des features (TW Clone, CC, EVOLVE) mais la maintenance ops (crons, disque, bloqueurs Chef) est ignorée. Le disque est à 3.6 GB (zone jaune) depuis 2 sessions.


### État disque
- Disponible : **3.6 GB** ⚠️ (seuil alerte : 3 GB — zone jaune)

### 3 Améliorations Proposées
1. **Fix crons en erreur maintenant** → `openclaw cron list` + diagnose + fix/delete → stoppe la dégradation silencieuse des routines quotidiennes
2. **Libérer de l'espace disque** → vider `/tmp/`, `~/Downloads/`, transcripts déjà traités → repasser > 5 GB (zone verte)
3. **CasaVolta → tester le flow EVOLVE complet** → lancer Phases 2-3 depuis EVOLVE Universel → valide que le pipeline fonctionne end-to-end avant de l'ouvrir à d'autres produits

---

## [08h06] Heartbeat — Maintenance crons + alerte disque

### Fait
- **3 crons corrigés en autonomie :**
  - `tasks-generator` → timeout augmenté 300s → 600s ✅
  - `evolve-weekly` → delivery `channel:last` → `channel:discord to:1475419314407473174` + best-effort ✅
  - `playlist-weekly-report` → même fix delivery ✅
- **Bug disk-watchdog découvert et corrigé :**
  - `df /` retourne des blocs 512-byte (pas des KB) → calcul était ×2 erroné
  - Espace réel : **1.8 GB** 🔴 (pas 3.6 GB)
  - Alerte Discord envoyée sur channel `1476994301803102312`
  - Seuil corrigé dans le cron prompt : `df -k /` → KB → ÷1048576

### Fichiers modifiés
- `~/.openclaw/jobs.json` → crons mis à jour via script Python (exec allowlist)
- Memory daily log

---

## [~14h00–19h51] Session principale — OMNIA EVOLVE Universel + Studio Clip

### Livré
- **EVOLVE Universel — Standardisation tous projets** : même architecture Research/Strategy/Execution/Optimization pour Nellio ET CasaVolta
- **Studio Clip refactorisé** : outil global sidebar (plus dans les onglets phases), sélecteurs dynamiques par projet
- **24 agents** : 4 phases × 6 agents chacune, system prompts spécialisés, chat persistant en mémoire JS, logs server-side
- **Research display unifié** : `eu-research-dynamic` avec 5 sub-tabs (Marché/Avatars/Angles/Concurrents/Hooks) + Dashboard Nellio
- **NELLIO_EMBEDDED** : données hardcodées pour Nellio (0 dépendance API)
- **PDF export** : `GET /api/evolve/universal/export-pdf/:slug`
- **Enrichissement hebdo** : endpoint + cron `evolve-weekly-enrich` couvrant tous les projets
- **Command Center** : 3 pipeline cards supprimées
- **OMNIA DA/UX overhaul** : palette Dark Pro, theme switcher 12 thèmes, sidebar 3 groupes, agents compact grid, phase chat zone

### Fix critique Studio Clip
- Bug : `</div>` manquant pour `aglayer-research` → layers imbriqués cassés
- Fix approche DOM-move → `cpanel-multiscene` reste dans `aglayer-creation` (permanent, pas de move)
- `showPage('multiscene')` → `targetPage='agents'` → active `page-agents` + `aglayer-creation`

### Architecture finale OMNIA index.html
- ~10 040+ lignes
- Palette CSS : `--bg:#09090f` / `--surface:#0f0f1a` / `--accent:#7c6ffd` / `--accent2:#ff5f7e` / `--accent3:#2fcfa4`
- Phase colors : `--phase-research:#7c6ffd` / `--phase-strategy:#2fcfa4` / `--phase-execution:#ff5f7e` / `--phase-optimization:#f59e0b`
- Theme system : `data-theme` sur `<html>` → 12 variantes CSS sans `!important`

### État 19h51
- HTML : ✅ F5 OMNIA suffisant
- Server endpoints (agents, PDF, enrichment) : ❌ inactifs jusqu'au restart Manuel Chef
- Disk : ~1.6 GB 🔴 — Arc cache ~4 GB récupérable (action manuelle Chef)

---

## [20h21] Studio Clip — Fix générateur UGC + Onglet Historique

### Root Cause générateur cassé
- `server.js` utilisait **OAuth token Anthropic** (`sk-ant-oat01-`) expiré
- Header incorrect : `Authorization: Bearer` + `anthropic-beta: oauth-2025-04-20`
- Fix : migré vers **clé API standard** (`sk-ant-api03-67IHi3-...`) avec header `x-api-key`
- Server redémarré automatiquement par LaunchAgent (confirmé via logs : 2 blocs startup)

### Fix server.js (lignes 25-28 + 106-124)
```
AVANT : ANTHROPIC_OAUTH_TOKEN = 'sk-ant-oat01-...' (expiré)
APRÈS : ANTHROPIC_API_KEY = 'sk-ant-api03-67IHi3-...' (stable, ne expire pas)
        callClaude → header x-api-key (format standard, pas OAuth)
```

### Fix frontend generateClipPrompt
- Remplacé `api()` helper par `fetch()` direct avec check `res.ok` + `res.status`
- Erreur affichée **inline** sous le textarea (div rouge) au lieu d'`alert()`
- Sauvegarde auto dans historique localStorage après chaque génération réussie

### Onglet Historique ajouté (index.html)
- Bouton `📁 Historique` dans `.studio-tabs` (après 🖼️ Images)
- Pane `studio-pane-history` : filtres (avatar/type/search) + cards générations
- Card : avatar · angle · sceneType · format · provider · date · script DE preview · prompt preview
- Actions : ↩ Réutiliser (reinjecte dans onglet UGC) / 🗑 Supprimer / ⬇️ CSV
- Persistance : `omnia_ugc_history_v2` localStorage · max 500 entrées · plus récent en premier
- Trigger save : (1) prompt UGC généré · (2) clip lancé via generateClipVariants
- Badge count temps réel sur l'onglet
- `showStudioTab('history')` → `renderUGCHistory()` + badge update

### Fonctions JS (index.html)
`saveUGCHistory` · `getUGCHistory` · `renderUGCHistory` · `clearUGCHistory`
`exportUGCHistory` · `reuseUGCHistoryEntry` · `deleteUGCHistoryEntry`

### État 20h30
- ✅ server.js : API key standard, server redémarré
- ✅ index.html : onglet Historique + générateur fix
- ⚠️ F5 OMNIA requis pour voir les changements frontend
- 🔴 Disk : ~1.8 GB (Arc cache ~4 GB → action manuelle Chef urgente)

---

## [21h30] UGC History — vidéos inline dans l'onglet Historique

### Implémentation
- `fillVariantCard()` → appelle `updateUGCHistoryClip(jobId, i, webmUrl, mp4Url)` dès qu'un clip finit
- `updateUGCHistoryClip()` : trouve l'entrée par `jobId` + `type === 'clip'` → ajoute `variants[]` → save localStorage → `renderUGCHistory()` si pane actif
- `renderUGCHistory()` : si `h.variants` → `<video>` inline (hover = lecture auto, clic = plein écran) + `⬇ MP4`
- Si clip en cours (pending) : `⏳ N clips en génération · Xmin écoulées` + `🔄 Actualiser` + `Voir dans UGC →`
- Bouton "Réutiliser" → renommé "↩ Prompt"

---

## [21h48] Video Cloner — Support Facebook Ad Library

### Problème
URL Facebook Ad Library → `needsTranscript: true` (seul YouTube supporté avant).

### Solution — video-cloner.js
- `detectUrlType(url)` : youtube / fb_adlibrary / facebook / tiktok / instagram / direct_video / unknown
- `firecrawlScrape(url)` : Firecrawl API (`fc-500f64750bc34036a6cf16ac4d7d2719`) → markdown cleaned
- `tryYtDlpTranscript(url)` : yt-dlp → mp3 → Whisper small DE → texte
- `tryExtractFacebookAd(url)` : Firecrawl d'abord, puis yt-dlp fallback
- Route `/analyze` : switch sur urlType → méthode d'extraction adaptée
- Message `needsTranscript` différencié : Facebook → guide 3 options + focus textarea

### Solution — index.html
- `clonerDetectUrl(value)` : hint temps réel sous le champ URL (méthode qui sera utilisée)
- Guide Facebook collapsible (`#cloner-fb-guide`) s'affiche pour Ad Library URLs
- Message contextuel quand extraction échoue

---

## [22h06] Video Cloner — Fix bouton "Cloner" sans effet

### Problème
Cliquer "🎬 Cloner pour Nellio" → rien (pas de requête dans les logs serveur).

### Root causes
1. **SDK Anthropic incompatible OAuth** : `@anthropic-ai/sdk` utilise `x-api-key` header → incompatible avec `sk-ant-oat01-` (OAuth) qui nécessite `Authorization: Bearer` + `anthropic-beta: oauth-2025-04-20`
2. **Blocage HeyGen** : route `/generate` attendait 20-30s les appels HeyGen (avatar list + voice list + video create) avant de répondre → timeout perçu comme "rien"

### Fix video-cloner.js
- Supprimé `@anthropic-ai/sdk` → remplacé par `callClaude()` fetch direct
- OAuth token actif : `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`
- Modèle : `claude-haiku-4-5` (rapide, économe)
- Route `/generate` : **répond immédiatement** avec `{ scriptDE, jobId }` après Claude → HeyGen en background (IIFE async non bloquant)

### Fix index.html generateClone()
- `setStatus()` helper null-safe → plus de throw silencieux si élément absent
- Check `res.ok` + `res.status` affiché en cas d'erreur HTTP
- Status différencié : "⏳ Génération script..." (distinct de l'analyse)

### Règle durable — Anthropic dans les routes Express
**NE PAS utiliser `@anthropic-ai/sdk`** avec un OAuth token (`sk-ant-oat01-`).
Toujours `callClaude()` fetch direct : `Authorization: Bearer` + `anthropic-beta: oauth-2025-04-20`.
Le SDK ne supporte pas ce format nativement.

### État 22h14
- ✅ video-cloner.js : OAuth + réponse immédiate + HeyGen background
- ✅ index.html : generateClone() null-safe + status clair
- ⚠️ OMNIA restart requis pour les changements video-cloner.js
  → `launchctl stop com.clawdbot.omnia && launchctl start com.clawdbot.omnia`
- 🔴 Disk : ~1.7 GB (critique — Arc cache ~4 GB récupérable, action manuelle Chef)
